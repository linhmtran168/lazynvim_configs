-- ============================================================================
-- TOML language module
-- ============================================================================

return {
  servers = { taplo = {} },
  formatters_by_ft = { toml = { "taplo" } },
  parsers = { "toml" },
  mason_tools = { "taplo" },
}
